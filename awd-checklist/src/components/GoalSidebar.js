import React from 'react';

const GoalSidebar = (props) => {
  return (<nav id="panel">
            <header>
              <h2> Learning Goals </h2>
            </header>
          </nav>);
}

export default GoalSidebar;
