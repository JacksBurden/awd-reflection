import React from 'react';

const Goal = (props) => {
  return (<main id="panel">
          <header>
            <h2> AWD Learning Goals Checklist </h2>
          </header>
        </main>);
}

export default Goal;
